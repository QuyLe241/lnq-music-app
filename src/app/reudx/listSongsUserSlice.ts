import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const listSongsUserSlice = createSlice({
  name: "listSongsUser",
  initialState,
  reducers: {},
});

export const {} = listSongsUserSlice.actions;

export default listSongsUserSlice.reducer;
